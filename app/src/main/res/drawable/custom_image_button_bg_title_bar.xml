<?xml version="1.0" encoding="utf-8"?>
<!--
  custom_image_button_bg_title_bar
  Rectangular ripple background for icon buttons placed in the title / action bar.
  Transparent at rest; the ripple is clipped to a rounded-rect mask.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/l_ripple_dark">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="4dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="4dp" />
        </shape>
    </item>
</ripple>
