<?xml version="1.0" encoding="utf-8"?>
<!--
  custom_image_button_bg
  Circular ripple background for standalone icon/image buttons.
  Fill is transparent so only the icon itself is visible at rest;
  the ripple provides touch feedback.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/l_ripple_light">
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white" />
        </shape>
    </item>
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
</ripple>
